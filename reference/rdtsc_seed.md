# Gets a seed for random number generation by using the processor time stamp (calling rdtsc in C).

Gets a seed for random number generation by using the processor time
stamp (calling rdtsc in C).

## Usage

``` r
rdtsc_seed()
```

## Value

An integer.
